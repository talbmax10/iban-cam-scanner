# دليل إعداد Capacitor لتحويل التطبيق إلى APK

## ✅ ما تم إنجازه

تم بناء تطبيق IBAN Scanner بالميزات التالية:
- ✅ واجهة عربية كاملة RTL
- ✅ كاميرا مباشرة + رفع صور
- ✅ OCR باستخدام Tesseract.js
- ✅ خوارزمية التحقق mod97 كاملة
- ✅ حفظ محلي للسجلات
- ✅ مشاركة وتصدير CSV
- ✅ تصميم احترافي بخط Cairo

## 📱 خطوات تحويل التطبيق إلى APK

### الخطوة 1: نقل المشروع إلى GitHub
1. اضغط على زر **"Export to Github"** في Lovable
2. انسخ المشروع من GitHub إلى جهازك:
```bash
git clone [YOUR_GITHUB_URL]
cd [PROJECT_NAME]
```

### الخطوة 2: تثبيت الاعتماديات
```bash
npm install
```

### الخطوة 3: بناء التطبيق
```bash
npm run build
```

### الخطوة 4: إضافة منصة أندرويد
```bash
npx cap add android
```

### الخطوة 5: مزامنة المشروع
```bash
npx cap sync android
```

### الخطوة 6: فتح المشروع في Android Studio
```bash
npx cap open android
```

**ملاحظة**: يجب تثبيت Android Studio أولاً من: https://developer.android.com/studio

### الخطوة 7: بناء APK من Android Studio

1. في Android Studio، انتقل إلى:
   ```
   Build > Build Bundle(s) / APK(s) > Build APK(s)
   ```

2. انتظر حتى يكتمل البناء

3. ستجد ملف APK في:
   ```
   android/app/build/outputs/apk/debug/app-debug.apk
   ```

## 🔧 التطوير المحلي مع Hot Reload

التطبيق مُعد للعمل مع hot reload مباشرة من sandbox Lovable. عند تشغيل التطبيق على الهاتف، سيتصل تلقائياً بـ:
```
https://37bfc874-b316-493e-8397-12d9ce806f26.lovableproject.com
```

لتعطيل hot reload والعمل محلياً:
1. افتح `capacitor.config.ts`
2. احذف قسم `server` بالكامل
3. قم ببناء المشروع مرة أخرى: `npm run build`
4. مزامنة التغييرات: `npx cap sync android`

## 🎨 الميزات المتوفرة

### 1. استخراج IBAN من الصور
- الكاميرا المباشرة
- رفع من المعرض
- OCR دقيق باستخدام Tesseract

### 2. التحقق من IBAN
- خوارزمية mod97 الرسمية
- دعم جميع الدول (50+ دولة)
- رسائل خطأ واضحة

### 3. الحفظ والمشاركة
- حفظ محلي آمن
- إضافة أسماء المالكين
- مشاركة عبر أي تطبيق
- تصدير CSV

### 4. واجهة احترافية
- تصميم عربي RTL
- خط Cairo الجميل
- ألوان تبعث الثقة
- Dark/Light mode

## 📋 الأذونات المطلوبة

التطبيق يحتاج الأذونات التالية (مضافة تلقائياً):
- **الكاميرا**: لالتقاط صور IBAN
- **التخزين**: لحفظ السجلات محلياً
- **الملفات**: لرفع الصور من المعرض

## 🔐 الأمان والخصوصية

- ✅ جميع البيانات محفوظة محلياً فقط
- ✅ لا يتم إرسال أي بيانات لخوادم خارجية
- ✅ OCR يعمل على الجهاز بدون اتصال
- ✅ التحقق من IBAN محلي 100%

## 🧪 اختبار التطبيق

### اختبار على المتصفح
يمكنك اختبار التطبيق مباشرة في Lovable قبل البناء:
1. استخدم زر الكاميرا
2. ارفع صورة IBAN تجريبية
3. تحقق من الوظائف

### اختبار على الهاتف
```bash
# تشغيل على جهاز متصل أو محاكي
npx cap run android
```

## 📝 ملاحظات مهمة

### للإصدار النهائي (Production):
1. قم بتغيير `server.url` في `capacitor.config.ts` أو احذفه
2. استخدم **Release Build** بدلاً من Debug:
   ```
   Build > Generate Signed Bundle / APK
   ```
3. أنشئ **keystore** لتوقيع التطبيق
4. اتبع إرشادات Google Play Console

### معلومات التطبيق:
- **App ID**: `app.lovable.37bfc874b316493e839712d9ce806f26`
- **App Name**: IBAN Scanner
- **اللغة**: العربية (RTL)
- **الحد الأدنى SDK**: Android 5.0+

## 🆘 حل المشاكل الشائعة

### 1. خطأ في البناء
```bash
# امسح الذاكرة المؤقتة
npm run build
npx cap sync android --force
```

### 2. الكاميرا لا تعمل
- تأكد من منح أذونات الكاميرا في إعدادات الهاتف
- في Android Studio، تحقق من `AndroidManifest.xml`

### 3. OCR بطيء
- Tesseract يحتاج وقتاً في أول استخدام
- استخدم صور واضحة بإضاءة جيدة

## 📚 موارد إضافية

- [Capacitor Docs](https://capacitorjs.com/docs)
- [Tesseract.js Docs](https://tesseract.projectnaptha.com/)
- [IBAN Validation Standard](https://en.wikipedia.org/wiki/International_Bank_Account_Number)

---

**مطور بواسطة Lovable** ❤️  
للدعم: اقرأ المقال الكامل عن Capacitor في مدونة Lovable
